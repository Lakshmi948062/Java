public class AreaOfRect {
    public static void main(String[] args) {
        int length = 10;
        int breadth = 5;
        int area = length * breadth; // Area of rectangle = length * breadth
        System.out.println("Area of the rectangle: " + area);
    }
}
